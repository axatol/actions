{
  "name": "actions",
  "private": true,
  "repository": "git@github.com:axatol/actions.git",
  "license": "MIT",
  "scripts": {
    "compile": "pnpm --recursive run --if-present compile",
    "stage": "pnpm --recursive run --if-present stage",
    "clean": "pnpm --recursive run --if-present clean"
  }
}
