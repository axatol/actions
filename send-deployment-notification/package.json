{
  "name": "@actions/send-deployment-notification",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "esbuild --bundle --external:esbuild --minify --sourcemap --platform=node --target=node20 --outfile=./index.js ./index.ts",
    "clean": "rm -f index.js index.js.map"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@actions/http-client": "^2.2.1",
    "esbuild": "^0.14.47"
  },
  "devDependencies": {
    "@types/node": "^20.7.0",
    "typescript": "^4.8.4"
  }
}
