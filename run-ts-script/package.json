{
  "name": "@actions/run-ts-script",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "esbuild --bundle --external:esbuild --minify --sourcemap --platform=node --target=node20 --outfile=./index.js ./index.ts",
    "clean": "rm -f index.js index.js.map"
  },
  "dependencies": {
    "@actions/core": "catalog:",
    "@actions/exec": "catalog:",
    "@actions/github": "catalog:",
    "@actions/glob": "catalog:",
    "@actions/io": "catalog:",
    "@octokit/plugin-request-log": "catalog:",
    "esbuild": "catalog:",
    "node-fetch": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
