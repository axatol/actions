{
  "name": "@actions/increment-semver",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "esbuild --bundle --minify --sourcemap --platform=node --target=node20 --outfile=./index.js ./index.ts",
    "clean": "rm -f index.js index.js.map"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.47",
    "typescript": "^4.8.4"
  }
}
